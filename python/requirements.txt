# HPXPy Python Dependencies
# =========================
#
# Install with: pip install -r requirements.txt
#
# These are the Python packages needed to BUILD and USE hpxpy.
# You must install these in the same Python environment (venv recommended)
# that you will use to configure CMake and run hpxpy.

# Build dependencies (required to compile _core extension)
numpy>=1.20
pybind11>=2.11

# Testing dependencies
pytest>=7.0
pytest-cov>=4.0
pytest-benchmark>=4.0
hypothesis>=6.0

# Tutorial/example dependencies (for running notebooks)
jupyter>=1.0
matplotlib>=3.5
scipy>=1.7
